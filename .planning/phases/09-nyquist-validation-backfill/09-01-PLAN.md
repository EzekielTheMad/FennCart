---
phase: 09-nyquist-validation-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/01-foundation-and-auth/01-VALIDATION.md
  - .planning/milestones/v1.0-phases/02-core-loop/02-VALIDATION.md
  - .planning/milestones/v1.0-phases/03-preference-system/03-VALIDATION.md
autonomous: true
requirements:
  - VAL-01

must_haves:
  truths:
    - "Phase 1 VALIDATION.md has nyquist_compliant: true and maps all 10 tests across test_health.py, test_wizard.py, test_oauth.py to SETUP-01 through SETUP-04 requirements"
    - "Phase 2 VALIDATION.md has nyquist_compliant: true and maps all tests across test_shopping_flow.py, test_cart_service.py, test_llm_matching.py, test_kroger_client.py, test_kroger_products.py to SRCH/CART/LLM requirements"
    - "Phase 3 VALIDATION.md has nyquist_compliant: true and maps all tests across test_receipt_parser.py, test_preference_service.py, test_preferences_flow.py to PREF-01 through PREF-06 requirements"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/01-foundation-and-auth/01-VALIDATION.md"
      provides: "Phase 1 nyquist-compliant validation"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v1.0-phases/02-core-loop/02-VALIDATION.md"
      provides: "Phase 2 nyquist-compliant validation"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v1.0-phases/03-preference-system/03-VALIDATION.md"
      provides: "Phase 3 nyquist-compliant validation"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Rewrite VALIDATION.md files for Phases 1, 2, and 3 from stubs to nyquist-compliant documents.

Purpose: These three phases cover the foundation (auth, wizard, OAuth), core shopping loop (search, match, cart), and preference system (receipts, NL chat, CRUD). Each has a stub VALIDATION.md with empty per-task maps and nyquist_compliant: false. The task is to map existing passing tests to phase requirements and produce complete, non-stub VALIDATION.md files.

Output: Three rewritten VALIDATION.md files with nyquist_compliant: true, complete per-task verification maps referencing real test files and functions, and wave_0_complete: true.
</objective>

<execution_context>
@C:\Users\Tony\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Tony\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-nyquist-validation-backfill/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 1 (Foundation and Auth) VALIDATION.md</name>
  <files>.planning/milestones/v1.0-phases/01-foundation-and-auth/01-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/01-foundation-and-auth/01-VALIDATION.md
    - tests/test_health.py
    - tests/test_wizard.py
    - tests/test_oauth.py
    - .planning/phases/09-nyquist-validation-backfill/09-RESEARCH.md (Phase 1 section)
  </read_first>
  <action>
Rewrite 01-VALIDATION.md from its current stub state to a complete nyquist-compliant document.

Set frontmatter:
- nyquist_compliant: true
- wave_0_complete: true
- status: approved

Per-Task Verification Map must include entries for every test function in the three test files, mapped to their requirement IDs:

Phase 1 requirements: SETUP-01 (wizard reachable), SETUP-02 (wizard config persisted), SETUP-03 (Kroger OAuth PKCE), SETUP-04 (silent token refresh), D-07 (missing config page)

Test file mapping (from research):
- tests/test_health.py: test_health_endpoint -> SETUP-01 (app startup proxy)
- tests/test_wizard.py: 4 tests -> SETUP-01, SETUP-02, D-02
- tests/test_oauth.py: 5 tests -> SETUP-03, SETUP-04, D-06

Update Test Infrastructure table to reflect actual pytest 8.4.2 (not the stub's "pytest 7.x").

Wave 0 Requirements: "None -- existing infrastructure covers all phase requirements." (conftest.py, test files all exist)

Manual-Only Verifications: Keep Kroger OAuth PKCE round-trip (SETUP-03 requires real browser) and Docker compose wizard (SETUP-01 requires Docker runtime).

Phase-scoped test command: python -m pytest tests/test_health.py tests/test_wizard.py tests/test_oauth.py -x -q

Do NOT rewrite or modify any test files. This is documentation only.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/milestones/v1.0-phases/01-foundation-and-auth/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "nyquist_compliant: true" in frontmatter
    - File contains "wave_0_complete: true" in frontmatter
    - File contains "test_health.py" in the per-task map
    - File contains "test_wizard.py" in the per-task map
    - File contains "test_oauth.py" in the per-task map
    - File contains "SETUP-01", "SETUP-02", "SETUP-03", "SETUP-04" requirement references
    - File contains phase-scoped test command: "python -m pytest tests/test_health.py tests/test_wizard.py tests/test_oauth.py"
  </acceptance_criteria>
  <done>Phase 1 VALIDATION.md is non-stub, nyquist_compliant: true, with complete test-to-requirement mapping for all 10 tests across 3 test files</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 2 (Core Loop) VALIDATION.md</name>
  <files>.planning/milestones/v1.0-phases/02-core-loop/02-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/02-core-loop/02-VALIDATION.md
    - tests/test_shopping_flow.py
    - tests/test_cart_service.py
    - tests/test_llm_matching.py
    - tests/test_kroger_client.py
    - tests/test_kroger_products.py
    - .planning/phases/09-nyquist-validation-backfill/09-RESEARCH.md (Phase 2 section)
  </read_first>
  <action>
Rewrite 02-VALIDATION.md from its current stub state to a complete nyquist-compliant document.

Set frontmatter:
- nyquist_compliant: true
- wave_0_complete: true
- status: approved

Phase 2 requirements: SRCH-01 (shopping page renders), SRCH-02 (product search + LLM match), SRCH-04 (confidence partitioning), LLM-01 (structured output), CART-01 (add to cart), CART-02 (local persistence on API failure)

Test file mapping (from research):
- tests/test_shopping_flow.py: 11 tests -> SRCH-01, SRCH-02, SRCH-04, SRCH-05, CART-01
- tests/test_cart_service.py: 9 tests -> SRCH-04, SRCH-02, CART-02, CART-03
- tests/test_llm_matching.py: 7 tests -> LLM-01
- tests/test_kroger_client.py: Kroger API client tests (infrastructure for SRCH-02)
- tests/test_kroger_products.py: Product search tests (infrastructure for SRCH-02)

Update Test Infrastructure table to reflect actual pytest 8.4.2.

Wave 0 Requirements: "None -- existing infrastructure covers all phase requirements."

Phase-scoped test command: python -m pytest tests/test_shopping_flow.py tests/test_cart_service.py tests/test_llm_matching.py tests/test_kroger_client.py tests/test_kroger_products.py -x -q

Do NOT rewrite or modify any test files. This is documentation only.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/milestones/v1.0-phases/02-core-loop/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "nyquist_compliant: true" in frontmatter
    - File contains "wave_0_complete: true" in frontmatter
    - File contains "test_shopping_flow.py", "test_cart_service.py", "test_llm_matching.py" in per-task map
    - File contains "SRCH-01", "SRCH-02", "SRCH-04", "LLM-01", "CART-01", "CART-02" requirement references
    - File contains phase-scoped test command
  </acceptance_criteria>
  <done>Phase 2 VALIDATION.md is non-stub, nyquist_compliant: true, with complete test-to-requirement mapping for all tests across 5 test files</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Phase 3 (Preference System) VALIDATION.md</name>
  <files>.planning/milestones/v1.0-phases/03-preference-system/03-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/03-preference-system/03-VALIDATION.md
    - tests/test_receipt_parser.py
    - tests/test_preference_service.py
    - tests/test_preferences_flow.py
    - .planning/phases/09-nyquist-validation-backfill/09-RESEARCH.md (Phase 3 section)
  </read_first>
  <action>
Rewrite 03-VALIDATION.md from its current stub state to a complete nyquist-compliant document.

Set frontmatter:
- nyquist_compliant: true
- wave_0_complete: true
- status: approved

Phase 3 requirements: PREF-01 (receipt PDF upload + parse), PREF-02 (parsed items editable), PREF-03 (preference upsert), PREF-04 (NL chat apply), PREF-05 (preference CRUD), PREF-06 (preferences injected into matching)

Test file mapping (from research):
- tests/test_receipt_parser.py: 4 tests -> PREF-01, PREF-02
- tests/test_preference_service.py: 16 tests -> PREF-03, PREF-05, PREF-06
- tests/test_preferences_flow.py: 11 tests -> PREF-01, PREF-04, PREF-05, PREF-06

PREF-02 (editable table UI) has no automated test and is correctly manual-only (HTMX visual behavior). Document it in Manual-Only Verifications section with: "Parsed receipt items displayed in editable table with brand/product fields" / "PREF-02" / "HTMX partial rendering requires browser interaction" / "Upload a receipt PDF, verify editable inputs appear for each parsed item".

Research Open Question 2 suggests adding a narrow integration test for PREF-02 (assert HTML partial contains input elements). If you determine this is worthwhile (takes < 5 minutes), add it to tests/test_preferences_flow.py. Otherwise, document PREF-02 as manual-only and move on.

Wave 0 Requirements: "None -- existing infrastructure covers all phase requirements."

Phase-scoped test command: python -m pytest tests/test_receipt_parser.py tests/test_preference_service.py tests/test_preferences_flow.py -x -q

Do NOT rewrite or refactor existing test files. Only add a test if PREF-02 coverage is deemed worthwhile.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/milestones/v1.0-phases/03-preference-system/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "nyquist_compliant: true" in frontmatter
    - File contains "wave_0_complete: true" in frontmatter
    - File contains "test_receipt_parser.py", "test_preference_service.py", "test_preferences_flow.py" in per-task map
    - File contains "PREF-01" through "PREF-06" requirement references
    - PREF-02 is documented either with an automated test or in Manual-Only Verifications
    - File contains phase-scoped test command
  </acceptance_criteria>
  <done>Phase 3 VALIDATION.md is non-stub, nyquist_compliant: true, with complete test-to-requirement mapping for all 31 tests across 3 test files, PREF-02 handled appropriately</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. grep "nyquist_compliant: true" across all three files
2. python -m pytest tests/test_health.py tests/test_wizard.py tests/test_oauth.py tests/test_shopping_flow.py tests/test_cart_service.py tests/test_llm_matching.py tests/test_kroger_client.py tests/test_kroger_products.py tests/test_receipt_parser.py tests/test_preference_service.py tests/test_preferences_flow.py -x -q
3. Verify no test files were modified (git diff tests/ should be empty or show only a new PREF-02 test)
</verification>

<success_criteria>
- Phases 1, 2, and 3 each have a non-stub VALIDATION.md with nyquist_compliant: true
- Every test function in the relevant test files is mapped to a requirement ID
- All referenced tests pass
- No existing tests were modified or broken
</success_criteria>

<output>
After completion, create `.planning/phases/09-nyquist-validation-backfill/09-01-SUMMARY.md`
</output>
