---
phase: 04-multi-provider-llm-and-settings
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - tests/test_settings.py
  - tests/test_llm_config.py
autonomous: true
requirements:
  - LLM-02
user_setup: []

must_haves:
  truths:
    - "get_active_llm_config() returns DB values when present and env fallback when not"
    - "Settings endpoints return 200 and correct HTML content"
    - "LLM save endpoint calls test_connection before persisting"
    - "LLM save with bad key returns error and does not persist"
    - "Ollama provider stores base_url and sets api_key_encrypted to None"
    - "Store search and select endpoints update AppConfig without touching wizard_step"
    - "Review mode preference persists and loads correctly"
  artifacts:
    - path: "tests/test_llm_config.py"
      provides: "Unit tests for get_active_llm_config()"
      contains: "test_llm_config_from_db"
    - path: "tests/test_settings.py"
      provides: "Integration tests for all settings endpoints"
      contains: "test_save_llm_settings"
  key_links:
    - from: "tests/test_settings.py"
      to: "app/routers/settings.py"
      via: "HTTP client requests to /settings/*"
      pattern: "client\\.(get|post).*settings"
    - from: "tests/test_llm_config.py"
      to: "app/services/llm_config.py"
      via: "direct function call"
      pattern: "get_active_llm_config"
---

<objective>
Write comprehensive tests for all Phase 4 functionality: unit tests for get_active_llm_config() (DB reads, Fernet decrypt, env fallback) and integration tests for all settings endpoints (LLM save with test-connection, store search/select, section switching, preferences save, Ollama config, hot-swap verification).

Purpose: Validates LLM-02 requirement — tests prove that provider switching works end-to-end, settings persist correctly, and the hot-swap mechanism (DB-authoritative reads) functions as designed.
Output: test_llm_config.py with unit tests, test_settings.py with integration tests. Full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-provider-llm-and-settings/04-CONTEXT.md
@.planning/phases/04-multi-provider-llm-and-settings/04-RESEARCH.md
@.planning/phases/04-multi-provider-llm-and-settings/04-VALIDATION.md
@.planning/phases/04-multi-provider-llm-and-settings/04-01-SUMMARY.md
@.planning/phases/04-multi-provider-llm-and-settings/04-02-SUMMARY.md

<interfaces>
<!-- Executor reads these to understand what to test -->

From app/services/llm_config.py:
```python
async def get_active_llm_config(db: AsyncSession) -> dict:
    """Returns dict: provider, model, api_key, ollama_base_url."""
```

From app/routers/settings.py (endpoints to test):
```
GET  /settings                      → settings hub page
GET  /settings/section/{section}    → HTMX partial (llm|store|account|preferences)
POST /settings/save-llm             → test + save LLM config
POST /settings/search-stores        → zip code store search
POST /settings/select-store         → select store by ID
POST /settings/save-preferences     → save review mode
```

From app/services/llm_service.py:
```python
async def test_connection(api_key, provider, model, base_url=None) -> tuple[bool, str]:
```

From tests/conftest.py (test fixtures):
```python
@pytest_asyncio.fixture: test_db (in-memory SQLite)
@pytest_asyncio.fixture: client (AsyncClient with DI overrides)
@pytest.fixture: test_settings (Settings with test values)
```

From app/models/config_model.py:
```python
class AppConfig(SQLModel, table=True):
    # ... all fields including new: llm_api_key_encrypted, llm_ollama_base_url, review_mode
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for get_active_llm_config() and integration tests for all settings endpoints</name>
  <files>tests/test_llm_config.py, tests/test_settings.py</files>
  <read_first>
    - tests/conftest.py
    - tests/test_cart_service.py
    - tests/test_shopping.py
    - app/services/llm_config.py
    - app/routers/settings.py
    - app/models/config_model.py
    - app/services/llm_service.py
    - app/services/oauth_manager.py
  </read_first>
  <action>
**1. Create `tests/test_llm_config.py`** — Unit tests for `get_active_llm_config()`:

Follow the same async test pattern as `tests/test_cart_service.py`: use `@pytest.mark.anyio` and the `test_db` fixture.

Tests to write:

**test_llm_config_env_fallback**: No AppConfig row in DB. Call `get_active_llm_config(test_db)`. Assert returns dict with `provider == "anthropic"`, `model == "claude-3-haiku-20240307"`, `api_key == "test_llm_key"` (from test_settings), `ollama_base_url == None`. Must patch `get_settings` to return `test_settings` fixture.

**test_llm_config_from_db**: Create AppConfig row with `llm_provider="openai"`, `llm_model="gpt-4o"`, `llm_api_key_encrypted=None`. Call `get_active_llm_config(test_db)`. Assert returns `provider == "openai"`, `model == "gpt-4o"`, `api_key == "test_llm_key"` (falls back to env since no encrypted key).

**test_llm_config_encrypted_key**: Create AppConfig row with `llm_provider="openai"`, `llm_model="gpt-4o"`, and `llm_api_key_encrypted` set to Fernet-encrypted `"sk-real-key"`. Patch `get_or_create_fernet` to return a Fernet instance with a known test key. Call `get_active_llm_config(test_db)`. Assert `api_key == "sk-real-key"`.

**test_llm_config_ollama**: Create AppConfig row with `llm_provider="ollama"`, `llm_model="llama3"`, `llm_api_key_encrypted=None`, `llm_ollama_base_url="http://localhost:11434"`. Assert returns `ollama_base_url == "http://localhost:11434"` and `api_key == "test_llm_key"` (env fallback, but won't be used by Ollama).

For Fernet testing, generate a test key: `from cryptography.fernet import Fernet; test_fernet = Fernet(Fernet.generate_key())`. Patch `app.services.llm_config.get_or_create_fernet` to return `test_fernet`. Encrypt with `test_fernet.encrypt(b"sk-real-key").decode()`.

**2. Create `tests/test_settings.py`** — Integration tests for settings endpoints:

Follow the same pattern as `tests/test_shopping.py`: use `client` fixture, seed AppConfig via helper.

Helper to seed config:
```python
async def _seed_config(db, **overrides):
    cfg = AppConfig(id=1, wizard_step="complete", wizard_complete=True, **overrides)
    db.add(cfg)
    await db.commit()
    return cfg
```

Tests to write:

**test_settings_page_renders**: `GET /settings` returns 200. Response body contains "LLM Provider", "Store", "Kroger Account", "Preferences" (sub-nav labels). Contains `id="settings-content"`.

**test_settings_section_llm**: `GET /settings/section/llm` returns 200. Body contains "LLM Provider", "Test connection and save", `name="provider"`.

**test_settings_section_store**: `GET /settings/section/store` returns 200. Body contains "Store location", "Find stores".

**test_settings_section_account**: `GET /settings/section/account` returns 200. Body contains "Kroger account", "Re-authorize with Kroger".

**test_settings_section_preferences**: `GET /settings/section/preferences` returns 200. Body contains "Preferences", "Save preferences", `name="review_mode"`.

**test_save_llm_settings**: Seed AppConfig. Patch `app.routers.settings.llm_service.test_connection` (or `app.services.llm_service.test_connection` depending on import path — check import in settings.py) to return `(True, "Connected")`. POST `/settings/save-llm` with form data `provider=openai&model=gpt-4o&api_key=sk-test123&custom_model=&ollama_base_url=`. Assert 200. Assert response contains "Provider updated successfully." Read AppConfig from DB: assert `llm_provider == "openai"`, `llm_model == "gpt-4o"`, `llm_api_key_encrypted is not None`.

**test_save_llm_invalid_key**: Seed AppConfig. Patch `test_connection` to return `(False, "Invalid API key")`. POST `/settings/save-llm` with form data. Assert 200. Assert response contains "Invalid API key". Read AppConfig from DB: assert `llm_provider` unchanged (still "anthropic").

**test_ollama_config**: Seed AppConfig. Patch `test_connection` to return `(True, "Connected")`. POST `/settings/save-llm` with `provider=ollama&model=__custom__&api_key=&custom_model=llama3&ollama_base_url=http://localhost:11434`. Assert 200 + success. Read AppConfig: `llm_provider == "ollama"`, `llm_model == "llama3"`, `llm_api_key_encrypted is None`, `llm_ollama_base_url == "http://localhost:11434"`.

**test_save_llm_blank_key_keeps_existing**: Seed AppConfig with `llm_api_key_encrypted` pre-set (encrypt a test value). Patch `test_connection` success. POST with `api_key=` (empty). Assert the existing `llm_api_key_encrypted` is preserved (not overwritten to None).

**test_search_stores**: Seed AppConfig. Patch `app.routers.settings.kroger_client.search_stores` to return `[{"locationId": "123", "name": "Fry's #123", "address": {"addressLine1": "123 Main St", "city": "Phoenix", "state": "AZ", "zipCode": "85001"}}]`. POST `/settings/search-stores` with `zip_code=85001`. Assert 200. Body contains "Fry's #123".

**test_select_store**: Seed AppConfig. POST `/settings/select-store` with `store_id=123&store_name=Fry's #123&store_zip=85001`. Assert 200. Assert body contains "saved". Read AppConfig: `store_id == "123"`, `store_name == "Fry's #123"`. Verify `wizard_step` is still "complete" (not mutated).

**test_save_preferences**: Seed AppConfig. POST `/settings/save-preferences` with `review_mode=full`. Assert 200. Assert body contains "Preferences saved". Read AppConfig: `review_mode == "full"`.

**test_llm_hotswap**: Seed AppConfig with `llm_provider="anthropic"`. Patch test_connection success. POST `/settings/save-llm` to switch to `openai`. Then call `get_active_llm_config(test_db)` and assert `provider == "openai"`. This proves the hot-swap path: settings save -> DB write -> next config read picks up new value.

For all integration tests, patch `get_or_create_fernet` to return a Fernet instance with a test key (to avoid file system access to /data/app.key in tests). Use `unittest.mock.patch`.

Approximate count: 4 unit tests + 12 integration tests = 16 tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_llm_config.py tests/test_settings.py -x -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_llm_config.py contains `test_llm_config_env_fallback`
    - tests/test_llm_config.py contains `test_llm_config_from_db`
    - tests/test_llm_config.py contains `test_llm_config_encrypted_key`
    - tests/test_llm_config.py contains `test_llm_config_ollama`
    - tests/test_settings.py contains `test_settings_page_renders`
    - tests/test_settings.py contains `test_save_llm_settings`
    - tests/test_settings.py contains `test_save_llm_invalid_key`
    - tests/test_settings.py contains `test_ollama_config`
    - tests/test_settings.py contains `test_save_llm_blank_key_keeps_existing`
    - tests/test_settings.py contains `test_search_stores`
    - tests/test_settings.py contains `test_select_store`
    - tests/test_settings.py contains `test_save_preferences`
    - tests/test_settings.py contains `test_llm_hotswap`
    - `python -m pytest tests/test_llm_config.py tests/test_settings.py -x` exits 0
    - `python -m pytest tests/ -x` exits 0 (full suite green)
  </acceptance_criteria>
  <done>16+ tests covering get_active_llm_config unit behavior and all settings endpoint integration paths. Full test suite green including pre-existing Phase 1/2 tests.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_llm_config.py tests/test_settings.py -v --tb=short` — all new tests pass
- `python -m pytest tests/ -x -q --tb=short` — full suite green (no regressions)
</verification>

<success_criteria>
- All unit tests for get_active_llm_config() pass (env fallback, DB read, Fernet decrypt, Ollama)
- All integration tests for settings endpoints pass (page render, section switching, LLM save/error, Ollama config, store search/select, preferences save, hot-swap proof)
- Full test suite (Phase 1 + 2 + 4 tests) is green
- LLM-02 requirement has comprehensive test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-provider-llm-and-settings/04-03-SUMMARY.md`
</output>
